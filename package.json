{
  "name": "logistics-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "cd backend && npm install && npm run start",
    "heroku-postbuild": "cd frontend && npm install && npm run build && cp -r build ../backend/"
  }
}
